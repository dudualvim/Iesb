#include <stdio.h>
#include <stdlib.h>
int Divide(int,int);

int main(int argc, char const *argv[])
{
    
    int  i, *ponteiro;

    printf("Digite o tamanho do vetor:");
    scanf("%d", i);

    int vet[i];

    for(i=0; i<vet[i]; i++){
        *ponteiro = vet[i] + 10;

    };

    printf("%d", &vet[i]);



};

